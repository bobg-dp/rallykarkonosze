# Frontend build-time environment.
# Backend runtime uses `backend.env` for production-like settings
# and `backend.local.env` for local testing.

PORT=8787

# Frontend build-time API URL.
# Local development: http://localhost:8787
# Production example: https://rallykarkonosze.rallydevil.com
VITE_API_BASE_URL=http://localhost:8787

# Allowed browser origins for cross-origin requests to the backend.
# Local development: http://localhost:5173
# Production example: https://rallykarkonosze.pl
# If you later expose the frontend on www as well, append it as a second origin.
CORS_ALLOWED_ORIGINS=http://localhost:5173

NOTICE_BOARD_API_URL=
NOTICE_BOARD_API_KEY=
NOTICE_BOARD_AUTH_HEADER=x-api-key